# Host에서 frontend/app.py를 직접 실행할 때 사용하는 Backend 주소입니다.
BACKEND_URL=http://127.0.0.1:8000

# compose.release.yml로 Registry Image를 받을 때만 사용합니다.
FRONTEND_IMAGE=<REGISTRY_ACCOUNT>/simple-multi-llm-frontend:1.0.0
